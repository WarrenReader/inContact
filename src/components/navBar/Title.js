import React from "react";
import styled from "styled-components";

const StyledTitle = styled.div`
  color: #fff;
  font-family: "Open Sans", sans-serif;
  font-size: 28px;
`;

const Title = props => <StyledTitle>inContact</StyledTitle>;

export default Title;
